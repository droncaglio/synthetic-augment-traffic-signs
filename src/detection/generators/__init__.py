"""Content generators (arms) that fill the shared allocation budget.

Each arm writes an augmented train tile set. See generators.base.ArmGenerator.
Implemented in Stage 2: zero_aug, da_only, real_duplicate, bg_photometric,
copy_paste, diffusion_bg.
"""
